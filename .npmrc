# @shopify/polaris 13 declares a peer dependency on React 18, but Next.js 15's App Router
# requires React 19. Polaris works on React 19 in practice (this app builds and renders on
# it), so accept the peer mismatch instead of failing the install. Vercel reads this file.
legacy-peer-deps=true
